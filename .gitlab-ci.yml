# DinD service is required for Testcontainers
include:
  - remote: "https://gitlab.com/gemseo/dev/ci-includes/-/raw/5/main.yml"

services:
  - name: docker:dind
    # explicitly disable tls to avoid docker startup interruption
    command: ["--tls=false"]

variables:
  # Instruct Testcontainers to use the daemon of DinD, use port 2375 for non-tls connections.
  DOCKER_HOST: tcp://docker:2375
  # Instruct Docker not to start over TLS.
  DOCKER_TLS_CERTDIR: ""
  # Improve performance with overlayfs.
  DOCKER_DRIVER: overlay2
  # GEMSEO version to install
  GEMSEO_PACKAGE_SPEC:
    value: ""
    description: "GEMSEO version to install (either from PyPI or from a local git repository)"

####################################################################################################
# Common settings for the test jobs.
####################################################################################################
.test-base:
  before_script:
    - curl -fsSL https://get.docker.com -o get-docker.sh
    - bash ./get-docker.sh
    - docker info
  rules:
    - if: $CI_COMMIT_REF_NAME !~ '/^(master|release-[0-9]+\.[0-9]+\.[0-9]+\w*)$/'
      GEMSEO_PACKAGE_SPEC: gemseo[all]
